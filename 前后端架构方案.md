# 思辩乐园——完整技术方案文档
## 一、前端框架选型与架构设计
### 1.1 推荐技术栈
| 层级 | 技术 | 说明 |
| ---- | ---- | ---- |
| 核心框架 | Vue 3 (Composition API) | 渐进式框架，生态成熟，适合动态交互 |
| 构建工具 | Vite | 极速冷启动，HMR 体验优秀 |
| 路由 | Vue Router 4 | 单页应用路由管理 |
| 状态管理 | Pinia | Vue 官方推荐，轻量且 TypeScript 友好 |
| HTTP 客户端 | Axios | 拦截器支持，便于统一处理 Token |
| UI 样式 | TailwindCSS | 保留现有设计风格 |
| 图标库 | Iconify | 继续使用 |
| Markdown 渲染 | marked + highlight.js | 博客内容展示 |

### 1.2 前端项目结构
```text
src/
├── assets/              # 静态资源
├── components/          # 公共组件
│   ├── layout/          # 导航栏、页脚、悬浮侧边栏
│   ├── common/          # 按钮、卡片、模态框
│   └── debate/          # 辩论相关组件
├── views/               # 页面视图
├── router/              # 路由配置
├── stores/              # Pinia 状态
├── api/                 # Axios 封装 + 接口定义
├── utils/               # 工具函数
└── App.vue              # 根组件


### 1.3 前后端整体架构
┌─────────────────────────────────────────────────────────────┐
│                         用户浏览器                           │
│                    (Vue 3 + Vite SPA)                       │
└─────────────────────────┬───────────────────────────────────┘
                          │ HTTPS
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                     Nginx (反向代理 + 静态资源)               │
│            /api/* 转发到 Django | / 返回前端入口              │
└─────────────────────────┬───────────────────────────────────┘
                          │
          ┌───────────────┴───────────────┐
          ▼                               ▼
┌─────────────────┐             ┌─────────────────┐
│  Django (后端)   │             │   静态文件 CDN   │
│  + DRF (API)    │◄───────────►│   (图片/上传)    │
└────────┬────────┘             └─────────────────┘
         │
         ▼
┌─────────────────┐     ┌─────────────────┐
│     MySQL       │     │     Redis       │
│   (主数据库)     │     │  (缓存/会话)    │
└─────────────────┘     └─────────────────┘

二、MySQL 数据库核心表设计
2.1 建表 SQL 语句
-- 1. 用户表
CREATE TABLE `users` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
    `username` VARCHAR(50) NOT NULL UNIQUE,
    `email` VARCHAR(100) NOT NULL UNIQUE,
    `password_hash` VARCHAR(255) NOT NULL,
    `avatar_url` VARCHAR(500) DEFAULT NULL,
    `bio` TEXT,
    `role` ENUM('user', 'moderator', 'admin') DEFAULT 'user',
    `is_active` BOOLEAN DEFAULT TRUE,
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    `last_login_at` DATETIME DEFAULT NULL,
    INDEX `idx_users_email` (`email`),
    INDEX `idx_users_username` (`username`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 2. 辩论话题表
CREATE TABLE `topics` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
    `creator_id` BIGINT UNSIGNED NOT NULL,
    `title` VARCHAR(200) NOT NULL,
    `description` TEXT,
    `category` VARCHAR(50) NOT NULL,
    `format` VARCHAR(30) NOT NULL DEFAULT '快嘴乱斗',
    `side_a_name` VARCHAR(50) NOT NULL DEFAULT '正方',
    `side_b_name` VARCHAR(50) NOT NULL DEFAULT '反方',
    `status` ENUM('open', 'closed', 'archived') DEFAULT 'open',
    `view_count` INT UNSIGNED DEFAULT 0,
    `comment_count` INT UNSIGNED DEFAULT 0,
    `hot_score` FLOAT DEFAULT 0,
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (`creator_id`) REFERENCES `users`(`id`) ON DELETE CASCADE,
    INDEX `idx_topics_status_hot` (`status`, `hot_score` DESC),
    INDEX `idx_topics_category` (`category`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 3. 话题投票表
CREATE TABLE `topic_votes` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
    `topic_id` BIGINT UNSIGNED NOT NULL,
    `user_id` BIGINT UNSIGNED NOT NULL,
    `side` ENUM('A', 'B') NOT NULL,
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (`topic_id`) REFERENCES `topics`(`id`) ON DELETE CASCADE,
    FOREIGN KEY (`user_id`) REFERENCES `users`(`id`) ON DELETE CASCADE,
    UNIQUE KEY `uk_topic_user` (`topic_id`, `user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 4. 辩论观点表
CREATE TABLE `debate_posts` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
    `topic_id` BIGINT UNSIGNED NOT NULL,
    `user_id` BIGINT UNSIGNED NOT NULL,
    `parent_id` BIGINT UNSIGNED DEFAULT NULL,
    `side` ENUM('A', 'B') NOT NULL,
    `content` TEXT NOT NULL,
    `evidence_url` VARCHAR(500) DEFAULT NULL,
    `upvotes` INT UNSIGNED DEFAULT 0,
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (`topic_id`) REFERENCES `topics`(`id`) ON DELETE CASCADE,
    FOREIGN KEY (`user_id`) REFERENCES `users`(`id`) ON DELETE CASCADE,
    FOREIGN KEY (`parent_id`) REFERENCES `debate_posts`(`id`) ON DELETE CASCADE,
    INDEX `idx_debate_posts_topic` (`topic_id`, `created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 5. 博客文章表
CREATE TABLE `blog_posts` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
    `author_id` BIGINT UNSIGNED NOT NULL,
    `title` VARCHAR(200) NOT NULL,
    `slug` VARCHAR(200) NOT NULL UNIQUE,
    `cover_image` VARCHAR(500) DEFAULT NULL,
    `content` LONGTEXT NOT NULL,
    `excerpt` VARCHAR(500) DEFAULT NULL,
    `category` VARCHAR(50) NOT NULL DEFAULT '逻辑体操',
    `tags` JSON DEFAULT NULL,
    `view_count` INT UNSIGNED DEFAULT 0,
    `like_count` INT UNSIGNED DEFAULT 0,
    `comment_count` INT UNSIGNED DEFAULT 0,
    `is_published` BOOLEAN DEFAULT TRUE,
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (`author_id`) REFERENCES `users`(`id`) ON DELETE CASCADE,
    INDEX `idx_blog_posts_published` (`is_published`, `created_at` DESC),
    INDEX `idx_blog_posts_slug` (`slug`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 6. 博客评论表
CREATE TABLE `blog_comments` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
    `post_id` BIGINT UNSIGNED NOT NULL,
    `user_id` BIGINT UNSIGNED NOT NULL,
    `parent_id` BIGINT UNSIGNED DEFAULT NULL,
    `content` TEXT NOT NULL,
    `upvotes` INT UNSIGNED DEFAULT 0,
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (`post_id`) REFERENCES `blog_posts`(`id`) ON DELETE CASCADE,
    FOREIGN KEY (`user_id`) REFERENCES `users`(`id`) ON DELETE CASCADE,
    FOREIGN KEY (`parent_id`) REFERENCES `blog_comments`(`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 7. 关注表
CREATE TABLE `follows` (
    `follower_id` BIGINT UNSIGNED NOT NULL,
    `followee_id` BIGINT UNSIGNED NOT NULL,
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`follower_id`, `followee_id`),
    FOREIGN KEY (`follower_id`) REFERENCES `users`(`id`) ON DELETE CASCADE,
    FOREIGN KEY (`followee_id`) REFERENCES `users`(`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 8. 收藏表
CREATE TABLE `favorites` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
    `user_id` BIGINT UNSIGNED NOT NULL,
    `target_type` ENUM('topic', 'blog_post', 'debate_post') NOT NULL,
    `target_id` BIGINT UNSIGNED NOT NULL,
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (`user_id`) REFERENCES `users`(`id`) ON DELETE CASCADE,
    UNIQUE KEY `uk_favorite` (`user_id`, `target_type`, `target_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 9. 勋章表
CREATE TABLE `badges` (
    `id` INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
    `name` VARCHAR(50) NOT NULL UNIQUE,
    `description` TEXT,
    `icon` VARCHAR(50) DEFAULT '🏅',
    `condition_json` JSON NOT NULL,
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 10. 用户勋章关联表
CREATE TABLE `user_badges` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
    `user_id` BIGINT UNSIGNED NOT NULL,
    `badge_id` INT UNSIGNED NOT NULL,
    `awarded_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (`user_id`) REFERENCES `users`(`id`) ON DELETE CASCADE,
    FOREIGN KEY (`badge_id`) REFERENCES `badges`(`id`) ON DELETE CASCADE,
    UNIQUE KEY `uk_user_badge` (`user_id`, `badge_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 11. 通知表
CREATE TABLE `notifications` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
    `recipient_id` BIGINT UNSIGNED NOT NULL,
    `type` VARCHAR(30) NOT NULL,
    `data` JSON NOT NULL,
    `is_read` BOOLEAN DEFAULT FALSE,
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (`recipient_id`) REFERENCES `users`(`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 12. 用户活动日志表
CREATE TABLE `activity_logs` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
    `user_id` BIGINT UNSIGNED NOT NULL,
    `action_type` VARCHAR(30) NOT NULL,
    `target_type` VARCHAR(20) DEFAULT NULL,
    `target_id` BIGINT UNSIGNED DEFAULT NULL,
    `data` JSON DEFAULT NULL,
    `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (`user_id`) REFERENCES `users`(`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

2.2 ER 关系图
erDiagram
    users {
        BIGINT id PK
        VARCHAR username
        VARCHAR email
        VARCHAR password_hash
        VARCHAR avatar_url
        TEXT bio
        ENUM role
        BOOLEAN is_active
        DATETIME created_at
        DATETIME last_login_at
    }

    topics {
        BIGINT id PK
        BIGINT creator_id FK
        VARCHAR title
        TEXT description
        VARCHAR category
        VARCHAR format
        VARCHAR side_a_name
        VARCHAR side_b_name
        ENUM status
        INT view_count
        INT comment_count
        FLOAT hot_score
        DATETIME created_at
        DATETIME updated_at
    }

    topic_votes {
        BIGINT id PK
        BIGINT topic_id FK
        BIGINT user_id FK
        ENUM side
        DATETIME created_at
    }

    debate_posts {
        BIGINT id PK
        BIGINT topic_id FK
        BIGINT user_id FK
        BIGINT parent_id FK
        ENUM side
        TEXT content
        VARCHAR evidence_url
        INT upvotes
        DATETIME created_at
    }

    blog_posts {
        BIGINT id PK
        BIGINT author_id FK
        VARCHAR title
        VARCHAR slug
        VARCHAR cover_image
        LONGTEXT content
        VARCHAR excerpt
        VARCHAR category
        JSON tags
        INT view_count
        INT like_count
        INT comment_count
        BOOLEAN is_published
        DATETIME created_at
        DATETIME updated_at
    }

    blog_comments {
        BIGINT id PK
        BIGINT post_id FK
        BIGINT user_id FK
        BIGINT parent_id FK
        TEXT content
        INT upvotes
        DATETIME created_at
    }

    follows {
        BIGINT follower_id PK,FK
        BIGINT followee_id PK,FK
        DATETIME created_at
    }

    favorites {
        BIGINT id PK
        BIGINT user_id FK
        ENUM target_type
        BIGINT target_id
        DATETIME created_at
    }

    badges {
        INT id PK
        VARCHAR name
        TEXT description
        VARCHAR icon
        JSON condition_json
        DATETIME created_at
    }

    user_badges {
        BIGINT id PK
        BIGINT user_id FK
        INT badge_id FK
        DATETIME awarded_at
    }

    notifications {
        BIGINT id PK
        BIGINT recipient_id FK
        VARCHAR type
        JSON data
        BOOLEAN is_read
        DATETIME created_at
    }

    activity_logs {
        BIGINT id PK
        BIGINT user_id FK
        VARCHAR action_type
        VARCHAR target_type
        BIGINT target_id
        JSON data
        DATETIME created_at
    }

    %% 关系定义
    users ||--o{ topics : "创建"
    users ||--o{ topic_votes : "投票"
    users ||--o{ debate_posts : "发表观点"
    users ||--o{ blog_posts : "发布文章"
    users ||--o{ blog_comments : "评论"
    users ||--o{ follows : "关注"
    users ||--o{ favorites : "收藏"
    users ||--o{ user_badges : "获得勋章"
    users ||--o{ notifications : "接收通知"
    users ||--o{ activity_logs : "产生行为"
    
    topics ||--o{ topic_votes : "被投票"
    topics ||--o{ debate_posts : "包含观点"
    
    blog_posts ||--o{ blog_comments : "包含评论"
    
    badges ||--o{ user_badges : "被授予"

三、Django REST API 接口设计
3.1 全局规范
基础 URL：/api/v1/
认证方式：JWT（请求头：Authorization: Bearer <token>）
统一响应格式：
{
    "code": 200,
    "msg": "success",
    "data": {}
}

3.2 接口清单
认证模块
方法	端点	说明	认证
POST	/auth/register/	注册	否
POST	/auth/login/	登录	否
POST	/auth/logout/	登出	是
GET	/auth/profile/	当前用户信息	是
PATCH	/auth/profile/	更新资料	是

用户主页
方法	端点	说明
GET	/users/{id}/	用户公开信息
GET	/users/{id}/stats/	统计数据
GET	/users/{id}/activity/	动态时间轴
GET	/users/{id}/badges/	勋章列表
GET	/users/{id}/heatmap/	创作热力图数据
POST	/users/{id}/follow/	关注
DELETE	/users/{id}/follow/	取消关注
GET	/users/{id}/followers/	粉丝列表
GET	/users/{id}/following/	关注列表

辩论话题
方法	端点	说明
GET	/topics/	话题列表
POST	/topics/	创建话题
GET	/topics/{id}/	话题详情
PUT/PATCH	/topics/{id}/	编辑话题
DELETE	/topics/{id}/	删除话题
POST	/topics/{id}/vote/	投票
GET	/topics/{id}/posts/	观点列表
POST	/topics/{id}/posts/	发表观点
POST	/debate-posts/{id}/upvote/	点赞观点
POST	/debate-posts/{id}/reply/	回复观点

博客系统
方法	端点	说明
GET	/blog/posts/	文章列表
POST	/blog/posts/	发布文章
GET	/blog/posts/{slug}/	文章详情
PUT/PATCH	/blog/posts/{id}/	编辑文章
DELETE	/blog/posts/{id}/	删除文章
POST	/blog/posts/{id}/like/	点赞
GET	/blog/posts/{id}/comments/	评论列表
POST	/blog/posts/{id}/comments/	发表评论
POST	/blog/comments/{id}/reply/	回复评论

收藏、通知
方法	端点	说明
GET	/favorites/	收藏列表
POST	/favorites/	添加收藏
DELETE	/favorites/{id}/	取消收藏
GET	/notifications/	通知列表
PATCH	/notifications/{id}/read/	标记已读
POST	/notifications/read-all/	全部已读

四、Django 项目架构
4.1 目录结构
debate_paradise/
├── manage.py
├── requirements.txt
├── config/
│   ├── settings/
│   │   ├── base.py
│   │   ├── development.py
│   │   └── production.py
│   ├── urls.py
│   └── wsgi.py
├── apps/
│   ├── users/
│   ├── debate/
│   ├── blog/
│   ├── notifications/
│   └── core/
├── utils/
└── media/

4.2 关键依赖
Django==4.2
djangorestframework==3.14
djangorestframework-simplejwt==5.2
django-cors-headers==4.0
mysqlclient==2.2
django-redis==5.3
celery==5.3
Pillow==10.0

五、开发路线图
环境搭建（Django + MySQL + Redis）
用户模块（注册、登录、JWT）
话题基础功能（CRUD、投票）
观点系统（发表、回复、点赞）
博客系统（文章、评论）
个人主页（数据聚合、关注、收藏）
通知与动态（Signal 自动记录）
前端对接（替换静态数据）
优化与部署（缓存、CDN、生产配置）